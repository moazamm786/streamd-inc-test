
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:streamedinc/main.dart';

void main() {
  // I've no idea which case is needed to be tested, as there are only 2 screens.
  // no action is being performed on buttons
  // that's why i didn't wrote any test case
}
